# Pi-to-Pi FIFO 任务队列升级开发计划

## 问题描述

在 Pi-to-Pi（coms）通信模式下，当 Pi-A 连续向 Pi-B 发送多条消息/任务时，存在**任务相互覆盖**的问题。具体表现为：

1. Pi-A 连续发送 Task-1、Task-2、Task-3 给 Pi-B
2. Pi-B 的 `inboundQueue` 中确实存储了所有任务
3. 但在 `agent_end` 事件处理时，只处理了队列中**最后一个未完成的任务**
4. 导致前序任务（Task-1、Task-2）永远不会被响应，造成消息丢失

## 根因分析

### 问题代码位置

文件：`extensions/coms.ts`

**入队逻辑（handlePrompt）**：
```typescript
function handlePrompt(socket: net.Socket, env: PromptEnvelope): void {
    // ... 省略部分代码 ...
    
    // 1. 将消息插入 inboundQueue
    const inbound: InboundContext = {
        msg_id: env.msg_id,
        hops: env.hops,
        sender_endpoint: env.sender_endpoint,
        sender_session: env.sender_session,
        response_schema: env.response_schema ?? null,
        fulfilled: false,
    };
    inboundQueue.set(env.msg_id, inbound);  // ✅ 正确：每条消息都有独立的 msg_id

    // 2. 使用 sendMessage 将消息注入接收者的下一轮
    pi.sendMessage(
        {
            customType: "coms-inbound",
            content: `[from ${env.sender_name} @ ${env.sender_cwd}]\n\n${env.prompt}`,
            display: true,
            details: {
                msg_id: env.msg_id,  // 关联 msg_id
                sender_session: env.sender_session,
                response_schema: env.response_schema ?? null,
            },
        },
        { deliverAs: "followUp", triggerTurn: true },
    );
    
    // ...
}
```

**出队逻辑（agent_end 事件）**：
```typescript
pi.on("agent_end", async (_event, ctx) => {
    // ❌ 问题所在：只取最后一个未完成的任务，而不是 FIFO 顺序处理
    const inbound = [...inboundQueue.values()].reverse().find((i) => !i.fulfilled);
    if (!inbound || !identity) return;
    
    // ... 处理单个任务 ...
    
    inbound.fulfilled = true;
    inboundQueue.delete(inbound.msg_id);
});
```

### 根本原因

1. **设计模型不匹配**：系统采用 "每轮处理一条消息" 的模型，但 `agent_end` 事件只触发一次，无法处理队列中积压的多条消息
2. **FIFO 队列名不副实**：虽然名为 `inboundQueue`，但实际没有实现 FIFO 的处理机制
3. **消息与响应的关联丢失**：`sendMessage` 触发的 LLM 回合只回应对应的消息，但没有机制确保每个入队消息都得到处理

### 附加问题

- 没有队列深度限制，可能导致内存无限增长
- 没有消息优先级机制
- 没有消息超时处理
- `currentInbound` 单例模式限制了并发处理能力

## 解决方案设计

### 方案 A：真 FIFO 顺序处理（推荐）

**核心思想**：维护一个真正的 FIFO 处理管道，确保每条消息按顺序得到完整的 LLM 处理回合。

**实现要点**：

1. **队列管理升级**
   - 将 `Map<string, InboundContext>` 改为 `Array<InboundContext>` 实现真正的 FIFO
   - 添加队列深度限制（可配置，默认 100）
   - 添加消息优先级标记

2. **消息处理器重构**
   ```typescript
   // 新的处理流程
   async function processQueue(): Promise<void> {
       while (inboundQueue.length > 0 && !isProcessing) {
           isProcessing = true;
           const inbound = inboundQueue[0]; // 取队列头部
           
           // 创建独立的 LLM 回合处理此消息
           await processSingleMessage(inbound);
           
           inboundQueue.shift(); // 移除已处理的消息
           inbound.fulfilled = true;
           isProcessing = false;
       }
   }
   ```

3. **LLM 回合绑定**
   - 为每条消息创建独立的 conversation context
   - 或者使用 `pi.sendMessage` 的回调机制确保消息-响应一一对应

4. **并发控制**
   - 添加 `isProcessing` 标志防止重入
   - 支持配置并发度（1 = 严格 FIFO，N = 并发处理前 N 条）

### 方案 B：批量响应模式

**核心思想**：将多条消息的响应合并处理，适用于需要汇总结果的场景。

**实现要点**：

1. 累积一定时间窗口内的消息或达到一定数量后批量处理
2. 批量生成响应并分别回传
3. 适用于消息之间有依赖关系的场景

### 方案 C：消息 ACK + 异步回调

**核心思想**：简化同步模型，采用完全异步的消息-回调模式。

**实现要点**：

1. 收到消息立即 ACK，不等待处理完成
2. 处理完成后通过独立的 response 通道回传结果
3. 需要客户端支持异步回调处理

## 子任务拆分（可并行开发）

### 任务 1：队列数据结构重构
**负责人**：Agent-1
**工作量**：中等
**依赖**：无

**任务内容**：
- [ ] 将 `inboundQueue: Map<string, InboundContext>` 改为 `Array<InboundContext>`
- [ ] 添加队列深度限制和溢出处理策略
- [ ] 添加消息优先级字段
- [ ] 更新 `handlePrompt` 函数以使用新队列结构
- [ ] 添加队列状态监控（深度、处理速率等）

**输出**：
- `extensions/coms-queue.ts` - 独立的队列管理模块
- 单元测试覆盖队列操作

---

### 任务 2：FIFO 消息处理器实现
**负责人**：Agent-2
**工作量**：高
**依赖**：任务 1

**任务内容**：
- [ ] 实现 `processQueue()` 循环处理函数
- [ ] 实现 `processSingleMessage()` 单消息处理逻辑
- [ ] 重构 `agent_end` 事件处理，改为由队列处理器驱动
- [ ] 添加处理状态机（idle → processing → responding → idle）
- [ ] 实现并发控制（`isProcessing` 标志 + 可配置并发度）
- [ ] 处理边界情况（如消息处理失败、超时等）

**输出**：
- 修改 `extensions/coms.ts` 的处理逻辑
- 状态转换图文档

---

### 任务 3：消息生命周期与上下文绑定
**负责人**：Agent-3
**工作量**：高
**依赖**：任务 1、任务 2

**任务内容**：
- [ ] 设计消息-上下文绑定机制，确保每条消息的处理独立
- [ ] 实现 `currentInbound` 栈（支持嵌套消息处理）
- [ ] 修改 `sendMessage` 调用，正确传递消息元数据
- [ ] 实现消息超时机制（可配置超时时间）
- [ ] 添加消息追踪日志（从接收到响应的完整链路）

**输出**：
- 消息生命周期状态图
- 增强的日志和追踪能力

---

### 任务 4：响应生成与回传重构
**负责人**：Agent-4
**工作量**：中等
**依赖**：任务 2

**任务内容**：
- [ ] 重构 `agent_end` 中的响应生成逻辑
- [ ] 确保响应与正确的 `msg_id` 关联
- [ ] 实现批量响应处理能力（为将来的批量模式预留）
- [ ] 添加响应失败重试机制（最多 3 次）
- [ ] 优化响应数据序列化（处理大响应）

**输出**：
- 响应处理模块
- 错误处理和重试逻辑

---

### 任务 5：配置与监控面板
**负责人**：Agent-5
**工作量**：中等
**依赖**：任务 1、任务 2

**任务内容**：
- [ ] 添加 FIFO 队列配置项到 `CliFlags`：
  - `queue_max_depth`: 队列最大深度（默认 100）
  - `queue_concurrency`: 并发处理数（默认 1 = FIFO）
  - `queue_timeout_ms`: 消息超时时间（默认 180000）
- [ ] 扩展 Widget 显示：
  - 当前队列深度
  - 待处理消息数
  - 处理速率（消息/分钟）
  - 平均处理时间
- [ ] 添加 `/coms-queue` 命令查看队列状态
- [ ] 添加队列告警（深度超过阈值时通知）

**输出**：
- 增强的 Widget UI
- 新的 slash 命令

---

### 任务 6：集成测试与回归验证
**负责人**：Agent-6
**工作量**：高
**依赖**：任务 1-5 完成

**任务内容**：
- [ ] 编写集成测试脚本：
  - 单发送者连续发送 N 条消息，验证全部收到响应
  - 多发送者并发发送，验证消息不丢失
  - 队列深度超限场景测试
  - 消息超时场景测试
  - 处理失败重试场景测试
- [ ] 进行性能基准测试（吞吐量、延迟）
- [ ] 回归测试现有功能（单消息场景）
- [ ] 编写测试报告和性能基准文档

**输出**：
- `tests/coms-fifo-test.ts` 测试脚本
- 测试报告文档

---

### 任务 7：文档与迁移指南
**负责人**：Agent-7
**工作量**：低
**依赖**：任务 6 完成

**任务内容**：
- [ ] 更新 `specs/pi-pi.md`，添加 FIFO 队列设计说明
- [ ] 编写用户文档：
  - 新配置项说明
  - 队列监控使用方法
  - 故障排查指南
- [ ] 编写开发者文档：
  - 新架构说明
  - 消息生命周期
  - 扩展点说明
- [ ] 编写迁移指南（从旧版本升级）

**输出**：
- 用户文档
- 开发者文档
- 迁移指南

## 开发顺序建议

```
Week 1:
├── 任务 1 (队列数据结构重构) ──┐
└── 任务 5 (配置与监控面板) ────┘ 并行开发

Week 2:
├── 任务 2 (FIFO 消息处理器) ───┐
└── 任务 3 (消息生命周期) ──────┘ 并行开发

Week 3:
├── 任务 4 (响应生成重构) ──────┐
└── 集成 tasks 1-5 的代码 ──────┘

Week 4:
├── 任务 6 (集成测试) ──────────┤
└── 任务 7 (文档) ──────────────┘
```

## 风险评估

| 风险 | 可能性 | 影响 | 缓解措施 |
|------|--------|------|----------|
| 重构引入新 Bug | 中 | 高 | 完善的集成测试 + 渐进式发布 |
| 性能下降 | 低 | 中 | 基准测试 + 性能优化专项 |
| 向后兼容性破坏 | 低 | 高 | 保持原有 API、添加功能开关 |
| 队列堆积导致内存溢出 | 中 | 高 | 深度限制 + 溢出策略 |

## 成功标准

1. ✅ Pi-A 连续发送 100 条消息，Pi-B 全部收到并正确响应
2. ✅ 消息响应顺序与发送顺序一致（FIFO）
3. ✅ 单消息场景的延迟不劣于当前版本（< 5%）
4. ✅ 所有现有功能回归测试通过
5. ✅ 队列深度、处理速率可在 Widget 中实时监控
6. ✅ 文档完整，用户无感知升级

---

**计划版本**：v1.0  
**创建日期**：2026-06-03  
**最后更新**：2026-06-03
